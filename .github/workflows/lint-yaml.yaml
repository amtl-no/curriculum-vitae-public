# yamllint disable rule:line-length
# SPDX-License-Identifier: CC-BY-4.0
# SPDX-FileCopyrightText: 2026 Arne Magnus Tveita Løken <212174338+amtl-no@users.noreply.github.com>
# yamllint enable rule:line-length
---
name: 'GitHub Actions Yaml linting'
on: # yamllint disable-line rule:truthy
  push:
    paths:
      - 'data/**.yaml'
      - '.github/**.yaml'
      - '.github/workflows/**.yaml'
      - '.yamllint.yaml'
  workflow_dispatch: # yamllint disable-line rule:truthy

jobs:
  yamllint:
    name: 'Lint ${{ matrix.target }}'
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        target:
          - 'data'
          - '.github/workflows'
          - '.github/dependabot.yaml'
          - '.yamllint.yaml'

    steps:
      - name: 'Checkout'
        uses: actions/checkout@v4
      - name: 'Yamllint ${{ matrix.target }}'
        uses: karancode/yamllint-github-action@master
        with:
          yamllint_file_or_dir: '${{ matrix.target }}'
          yamllint_strict: true
          yamllint_comment: false
